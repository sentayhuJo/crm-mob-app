/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import { AppRegistry } from 'react-native';
import App from './src/app';

// Load the application CRM and pont to app
AppRegistry.registerComponent('crm', () => App);
